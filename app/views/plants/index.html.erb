<div class="hero-banner">
 <img src="banner.jpg" class="banner">
 <h1>ALL PLANTS</h1>
</div>
<div class="container">
  <div class="cards">
  <% @plants.each do |plant| %>
    <div class="card">
    <img src="<%= plant.image_url %>">
    <%= plant.name %><br>
    <%=link_to "plant queries", plant_queries_path(plant) %> 
    </div> 
  <%end%>    

  </div>


</div>